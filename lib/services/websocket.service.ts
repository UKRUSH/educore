// WebSocket service stub — Socket.io for chat (placeholder)

// In production: initialize Socket.io server and handle chat events.
// For now, chat is handled via API (e.g. POST /api/support/chat for community posts).

export function getSocketServer() {
  return null;
}

export function broadcastToRoom(): void {
  // Stub: no-op
}
