// Email service stub — Nodemailer / Resend integration placeholder

export async function sendEmail(to: string, subject: string, text: string): Promise<void> {
  console.log("[Email stub]", { to, subject, text });
}
